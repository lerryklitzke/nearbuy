import { loginSchema, LoginType } from './login.validation';
import { registerSchema, RegisterType } from './register.validation';

export {
  loginSchema, LoginType,
  registerSchema, RegisterType,
}
